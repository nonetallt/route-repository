import Route from '../src/Route'

describe('constructor', () => {

    it('initializes the object', () => {

        const route: Route = new Route('soo');

		/* expect(new Route).toBeInstanceOf(Route) */
    })
})

describe('register', () => {

})
