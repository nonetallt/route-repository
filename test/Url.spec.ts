import Url from '../src/Url'

describe('Url', () => {

    describe('parse', () => {

        it('finds all parameters', () => {
        })
    })

    describe('foo', () => {
        const url = new Url('/foo')

        url.bindObject({foo: 'foo'})
    })
})
